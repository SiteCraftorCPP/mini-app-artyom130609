import service from "@/assets/icon/services/services.webp";
import subService from "@/assets/icon/services/sub_services.webp";

import type { ServiceCardIconToken } from "@/entities/service";

export const SERVICE_ICONS: Record<ServiceCardIconToken, string> = {
  service,
  subService,
};
